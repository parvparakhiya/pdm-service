# Serving image. Exact pins, no `>=`.
#
# The previous requirements.txt used `>=` for every package, so two builds a
# week apart shipped different code and a new lightgbm or shap release could
# break production silently. Treat a change here like any other code change.
#
# These are direct dependencies, pinned by hand. Transitive versions are not
# locked; before go-live this should become pip-compile output from an `.in`
# file so the whole tree is reproducible.
#
# starlette is pinned explicitly even though fastapi pulls it in. It is the
# ASGI layer every request passes through, and three HIGH CVEs against 0.41.3
# (Range-header DoS, SSRF via UNC paths in StaticFiles, form-limit bypass)
# reached this image only because it was left to float. A dependency on the
# request path gets its own line.
#
# NOT PRESENT, and deliberately so:
#   lightgbm, scikit-learn, shap  -> were pulled in only to unpickle a SHAP
#       TreeExplainer at startup. That inflated the "dependency-light ONNX
#       serving image" by several hundred megabytes, tied the artifact to the
#       exact training-time library versions, and made `joblib.load` an
#       arbitrary-code-execution surface on the serving path. Explanations
#       now come from the specification rules, which are exact and need no
#       libraries.
#   pandas -> the domain layer is plain Python floats. Nothing in the request
#       path needs a DataFrame once the rolling-window features are gone.

fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
prometheus-client==0.26.0
